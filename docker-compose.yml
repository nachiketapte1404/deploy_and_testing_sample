services:
  backend:
    build: ./deploytesting
    ports:
      - "8080:8080"
    networks:
      - app-network

  frontend:
    build: ./task-manager-ui
    ports:
      - "4200:80"
    networks:
      - app-network
    depends_on:
      - backend

networks:
  app-network:
    driver: bridge